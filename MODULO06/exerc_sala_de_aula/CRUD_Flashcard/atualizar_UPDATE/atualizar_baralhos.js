import PromptSync from "prompt-sync";
import baralhos from "../dados/baralho.js";
const prompt = PromptSync();

function atualizar_baralhos() {
  for (let i = 0; i < baralhos.length; i++) {
  
  if (baralho.id === baralhos[i].id) {
   baralhos[i].titulo=baralho.titulo 
   return baralho 
  }

}
if (!controle) {
  console.log("Baralho nao encontrado");
 }}


export default atualizar_baralhos
